[package]
name = "auth-example"
version = "0.1.0"
edition = "2021"

[dependencies]
uncovr = { path = "../.." }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
