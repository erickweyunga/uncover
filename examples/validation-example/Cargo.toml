[package]
name = "validation-example"
version = "0.1.0"
edition = "2021"

[dependencies]
uncovr = { path = "../.." }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
schemars = "0.8"
validator = "0.18"
